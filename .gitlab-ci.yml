stages:
    - build
    - policies

before_script:
     - if [ ! -d "spack" ]; then
            git clone https://github.com/spack/spack.git spack;
          fi
     - pwd
     - ls
     - ls spack
cache:
        paths:
            - spack/
job 1:
    stage: build
    script:
        - echo "building"
        - pwd
        - ls
        - if [ ! -d "xsdk-ci-test" ]; then
            git clone https://github.com/xsdk-project/xsdk-ci-test.git xsdk-ci-test;
          fi
        - pwd
        - more README
        - cd spack/bin/
        - sh ../../xsdk-ci-test/spack xSDK-install.sh
    cache:
        paths:
            - xsdk-ci-test/
job 2:
    stage: policies
    script:
        - echo "policies"
        - more README
    cache:
        paths:
            - spack/

